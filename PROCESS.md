# FlowIT CRM 프로세스 메뉴얼

> 이 문서는 FlowIT CRM의 각 기능이 **내부적으로 어떻게 동작하는지** 단계별 프로세스를 설명합니다.  
> 시스템 담당자, 개발자, 고급 사용자를 위한 기술 참고 자료입니다.

---

## 목차

1. [고객 통합 식별 프로세스](#1-고객-통합-식별-프로세스)
2. [RFM 점수 계산 프로세스](#2-rfm-점수-계산-프로세스)
3. [캠페인 발송 프로세스](#3-캠페인-발송-프로세스)
4. [여정 자동화 엔진 프로세스](#4-여정-자동화-엔진-프로세스)
5. [VOC 분석 프로세스](#5-voc-분석-프로세스)
6. [주문 수집 프로세스](#6-주문-수집-프로세스)
7. [유통기한 알림 프로세스](#7-유통기한-알림-프로세스)
8. [API 헬스 체크 프로세스](#8-api-헬스-체크-프로세스)
9. [자동화 배치 일정 (Cron)](#9-자동화-배치-일정-cron)
10. [사용자 초대 및 온보딩 프로세스](#10-사용자-초대-및-온보딩-프로세스)
11. [감사 로그 기록 프로세스](#11-감사-로그-기록-프로세스)
12. [영업 파이프라인 단계 변경 프로세스](#12-영업-파이프라인-단계-변경-프로세스)
13. [전체 데이터 흐름](#13-전체-데이터-흐름)

---

## 1. 고객 통합 식별 프로세스

**목적**: 여러 쇼핑몰/채널에서 동일 고객이 구매하더라도 하나의 고객 레코드로 통합합니다.

**실행 시점**: 웹훅으로 주문이 수신될 때마다 자동 실행

### 식별 단계 (5단계, 순서대로 시도)

```
주문 데이터 수신 (channel, channelUserId, email, phone, name, address)
         │
         ▼
① 채널 ID 정확 매칭 ────────── 일치 → 기존 고객 반환
         │ 불일치
         ▼
② 이메일 정규화 매칭 ────────── 일치 → 채널 ID 추가 연결 후 반환
         │ 불일치
         ▼
③ 전화번호 정규화 매칭 ──────── 일치 → 채널 ID 추가 연결 후 반환
         │ 불일치
         ▼
④ 이름 + 주소 복합 매칭 ──────── 일치 → 채널 ID 추가 연결 후 반환
   (신뢰도 0.75)         불일치
         │
         ▼
⑤ 신규 고객 생성 → 식별자 3개 동시 등록 (채널ID + 이메일 + 전화번호)
```

### 각 단계 상세 설명

| 단계 | 조건 | 신뢰도 | 설명 |
|------|------|--------|------|
| ① 채널 ID | channel + channelUserId 완전 일치 | 100% | 동일 채널에서 같은 계정으로 재구매 |
| ② 이메일 | 정규화된 이메일 일치 | 100% | 소문자 변환 + 공백 제거 후 비교 |
| ③ 전화번호 | 정규화된 전화번호 일치 | 100% | 숫자만 추출 후 비교 |
| ④ 이름+주소 | 이름과 배송지 주소 동시 일치 | 75% | 동명이인 가능성 존재 |
| ⑤ 신규 생성 | 어느 단계도 일치하지 않음 | - | 처음 방문한 신규 고객 |

### 데이터 정규화 규칙

```
이메일 정규화:
  "User@EXAMPLE.COM " → "user@example.com"
  (소문자 변환 + 앞뒤 공백 제거)

전화번호 정규화:
  "010-1234-5678" → "01012345678"
  "+82-10-1234-5678" → "821012345678"
  (숫자만 추출)
```

### 신규 고객 생성 시 등록되는 식별자

```
CustomerIdentity 3개 레코드 생성:
  1. identifierType: 'channel_id'  → 채널 계정 ID
  2. identifierType: 'email'       → 정규화된 이메일
  3. identifierType: 'phone'       → 정규화된 전화번호
```

---

## 2. RFM 점수 계산 프로세스

**목적**: 모든 고객을 구매 패턴에 따라 자동으로 세그먼트(VIP, 이탈위험 등)로 분류합니다.

**실행 시점**: 매일 새벽 3시 (자동), 또는 수동 트리거

### RFM 점수 산출 기준

#### R (Recency) — 최근 구매일

| 마지막 구매 이후 경과일 | 점수 |
|----------------------|------|
| 30일 이내 | 5점 |
| 31 ~ 60일 | 4점 |
| 61 ~ 90일 | 3점 |
| 91 ~ 180일 | 2점 |
| 180일 초과 | 1점 |

#### F (Frequency) — 구매 빈도

| 총 구매 횟수 | 점수 |
|------------|------|
| 5회 이상 | 5점 |
| 4회 | 4점 |
| 3회 | 3점 |
| 2회 | 2점 |
| 1회 | 1점 |

#### M (Monetary) — 누적 구매액

| 총 구매 금액 | 점수 |
|------------|------|
| 500,000원 이상 | 5점 |
| 300,000 ~ 499,999원 | 4점 |
| 150,000 ~ 299,999원 | 3점 |
| 50,000 ~ 149,999원 | 2점 |
| 50,000원 미만 | 1점 |

> **포함 주문 상태**: PAID(결제완료), SHIPPED(배송중), DELIVERED(배송완료)만 집계  
> **분석 기간**: 최근 365일 주문 데이터 기준

### 세그먼트 결정 로직

```
RFM 합계 = R + F + M (최소 3점 ~ 최대 15점)

세그먼트 판단 순서 (위에서부터 순차 평가):

① 신규 고객 (new)
   조건: 가입일로부터 30일 미만 AND 구매 횟수 ≤ 2회

② VIP
   조건: RFM 합계 ≥ 13점

③ 이탈 위험 (churn_risk)
   조건: R = 1점 AND 구매 횟수 ≤ 2회

④ 충성 고객 (loyal)
   조건: F ≥ 5점 AND R ≥ 3점

⑤ 일반 (normal)
   조건: 위 조건에 해당하지 않는 모든 경우
```

### 처리 방식

- **처리 단위**: 1,000명씩 청크 분할
- **병렬 처리**: 청크 내 고객은 동시에 처리
- **업데이트 대상**: 모든 활성 고객 (is_active = true)

### 처리 결과 (DB 업데이트)

```
customers 테이블 업데이트:
  rfm_score: { "r": 4, "f": 3, "m": 5, "total": 12 }
  segment:   "vip" | "loyal" | "new" | "churn_risk" | "normal"
  ltv:       850000  (누적 총 구매액)
```

---

## 3. 캠페인 발송 프로세스

**목적**: 특정 고객 세그먼트에 이메일, 알림톡, SMS 메시지를 발송합니다.

**실행 시점**: 관리자/매니저가 "발송" 버튼 클릭 시 즉시 실행

### 발송 흐름

```
발송 버튼 클릭
    │
    ▼
① 상태 검증
   ├─ 상태가 'draft' 또는 'scheduled'인가?
   │   NO → 오류 반환 (이미 발송됨 또는 진행 중)
   └─ YES → 계속 진행
    │
    ▼
② 캠페인 상태를 'sending'으로 변경
    │
    ▼
③ 수신 대상 고객 조회
   ├─ 세그먼트 필터 설정됨? → 해당 세그먼트 고객만
   └─ 세그먼트 미설정? → 모든 활성 고객
    │
    ▼
④ A/B 테스트 설정 확인
   ├─ abEnabled = true?
   │   ├─ 고객별 무작위 배정 (abRatioA% → 메시지A, 나머지 → 메시지B)
   │   └─ variant 기록 ('A' 또는 'B')
   └─ abEnabled = false? → 모든 고객에게 메시지A 발송
    │
    ▼
⑤ 채널별 발송
   ├─ EMAIL → Resend API 호출
   ├─ KAKAO_ALIMTALK → [추후 구현]
   └─ SMS → [추후 구현]
    │
    ▼
⑥ 발송 결과 기록 (campaign_sends 테이블)
   ├─ 성공: status = 'sent'
   └─ 실패: status = 'bounced'
    │
    ▼
⑦ 캠페인 상태를 'done'으로 변경 + 발송 시각 기록
```

### A/B 테스트 처리 예시

```
abRatioA = 70 (메시지A 70%, 메시지B 30%)

고객 1000명 발송 시:
  약 700명 → 메시지A (variant='A')
  약 300명 → 메시지B (variant='B')

결과는 campaign_sends 테이블에서 분석:
  SELECT variant, COUNT(*), SUM(revenue_attr) 
  FROM campaign_sends WHERE campaign_id = ?
```

### 이메일 발송 설정

```
발신자: 환경변수 EMAIL_FROM (기본값: noreply@flowit.kr)
제목: 캠페인명
본문: HTML 형식 메시지 (A 또는 B)
```

### 캠페인 생성 시 abTest 구조

```json
{
  "abTest": {
    "messageA": "안녕하세요! 이번 달 VIP 특가를 확인해보세요.",
    "messageB": "지금 구매하면 20% 할인! 놓치지 마세요.",
    "abEnabled": true,
    "abRatioA": 50
  }
}
```

---

## 4. 여정 자동화 엔진 프로세스

**목적**: 고객이 특정 조건(트리거)을 충족하면 자동으로 다단계 마케팅 액션을 수행합니다.

**실행 시점**: 1시간마다 자동 실행

### 여정 스텝 유형

| 스텝 타입 | 동작 | 설정 값 |
|-----------|------|---------|
| `send_message` | 캠페인 메시지 발송 | `campaignId` |
| `delay` | 다음 스텝까지 대기 | `days` (일수) |
| `condition` | 조건 분기 (true/false 경로) | `check`, `days`, `true[]`, `false[]` |

### 여정 실행 흐름

```
매시간 실행
    │
    ▼
① 실행 대상 조회: 상태가 'active'인 모든 여정 등록 건
    │
    ▼
② 각 등록 건별 처리:

   현재 스텝 번호(currentStep) ≥ 전체 스텝 수?
   ├─ YES → 상태를 'completed'로 변경 후 종료
   └─ NO → 현재 스텝 실행
    │
    ▼
③ 스텝 유형에 따라 분기:

   [send_message]
     캠페인 발송 기록 생성 → 다음 스텝으로 이동
   
   [delay]
     현재 스텝 번호만 증가 → 다음 실행 주기까지 대기
   
   [condition]
     ├─ 조건 평가 (evaluateCondition)
     │    check='purchased' → 최근 N일 내 구매 여부
     │    check='opened_campaign' → 최근 N일 내 캠페인 오픈 여부
     │    check='is_segment' → 특정 세그먼트 소속 여부
     ├─ 조건 만족(true) → true 브랜치 스텝 실행
     └─ 조건 미충족(false) → false 브랜치 스텝 실행
```

### 여정 정의 JSON 예시 (3단계 여정)

```json
{
  "steps": [
    {
      "type": "send_message",
      "config": { "campaignId": "welcome-email-campaign-id" }
    },
    {
      "type": "delay",
      "config": { "days": 3 }
    },
    {
      "type": "condition",
      "config": {
        "check": "purchased",
        "days": 3,
        "true": [
          { "type": "send_message", "config": { "campaignId": "thank-you-campaign-id" } }
        ],
        "false": [
          { "type": "send_message", "config": { "campaignId": "reminder-campaign-id" } }
        ]
      }
    }
  ]
}
```

**위 여정 실행 흐름 해석:**
1. 웰컴 이메일 즉시 발송
2. 3일 대기
3. 3일 내 구매 여부 확인 → 구매했으면 감사 메일, 미구매면 리마인더 발송

### 여정 등록(Enrollment) 상태 관리

| 상태 | 의미 |
|------|------|
| `active` | 여정 진행 중 |
| `completed` | 모든 스텝 완료 |
| (없음) | 오류 발생 시 다음 주기에 재시도 |

---

## 5. VOC 분석 프로세스

**목적**: 수집된 고객 리뷰를 AI로 자동 분석하여 감정과 키워드를 추출하고, 부정 리뷰 급증 시 경보를 발송합니다.

**실행 시점**: 매일 새벽 2시 (자동)

### 분석 흐름

```
매일 02:00 실행
    │
    ▼
① 미분석 리뷰 최대 500건 조회 (analyzedAt = null인 건)
    │
    ▼
② 각 리뷰별 Claude AI 감정 분석:

   입력: 리뷰 본문 텍스트
   
   AI 응답 형식 (JSON):
   {
     "sentiment": "positive" | "neutral" | "negative",
     "tags": ["보습력", "흡수력", "향기", ...]
   }
    │
    ▼
③ 분석 결과 저장:
   voc_reviews.sentiment = "positive"
   voc_reviews.tags = ["보습력", "흡수력"]
   voc_reviews.analyzed_at = 현재 시각
    │
    ▼
④ 이상 징후 탐지 (최근 7일 기준):

   상품별 분석:
   ├─ 전체 리뷰 수 집계
   ├─ 부정 리뷰 수 집계
   └─ 부정 비율 계산 (부정 수 / 전체 수)
   
   임계값 초과 여부 확인:
   ├─ 부정 비율 > 15%, 또는
   └─ 부정 리뷰 수 ≥ 10건
    │
    ▼
⑤ 이상 징후 감지 시:
   ├─ 해당 상품의 부정 리뷰에 is_alert = true 설정
   └─ Slack 알림 발송:
      "⚠️ VOC 이상 징후: SKU {상품ID} - 부정 리뷰 {N}건 ({비율}%)"
```

### 이상 징후 판단 기준

| 상황 | 예시 | 결과 |
|------|------|------|
| 7일 리뷰 50건, 부정 9건 | 비율 18% > 15% | ⚠️ 경보 발생 |
| 7일 리뷰 100건, 부정 12건 | 건수 12 ≥ 10건 | ⚠️ 경보 발생 |
| 7일 리뷰 100건, 부정 8건 | 비율 8%, 건수 8건 | 정상 |

### Slack 알림 예시

```
⚠️ VOC 이상 징후: SKU cm3a9x2kl0001... - 부정 리뷰 12건 (비율 18.5%)
```

> Slack Webhook URL이 환경변수에 설정되지 않은 경우 알림 없이 DB만 업데이트됩니다.

---

## 6. 주문 수집 프로세스

**목적**: 각 판매 채널(자체 쇼핑몰, 쿠팡 등)에서 발생한 주문을 수집하여 통합 데이터베이스에 저장합니다.

### 단계 1 — 웹훅 수신 (실시간)

```
채널에서 주문 발생 시 자동 전송:

자체몰 → POST /api/webhooks/own-mall
쿠팡   → POST /api/webhooks/coupang

처리:
  원본 JSON 데이터를 channel_orders_raw 테이블에 즉시 저장
  └─ channel: 'OWN_MALL' 또는 'COUPANG'
     raw_json: 채널 원본 형식 그대로
     processed: false (미처리 상태)
```

### 단계 2 — 주문 정규화 처리 (배치)

```
POST /api/orders/sync (수동 또는 예약 실행)

① channel_orders_raw에서 processed=false 레코드 조회

② 각 레코드별:
   ├─ 채널 원본 형식 파싱 (채널별 어댑터)
   ├─ 고객 식별 (Identity Resolution 프로세스 적용)
   ├─ 통합 Order 스키마로 변환
   │   {channel, channelOrderId, totalAmount, orderedAt, status}
   ├─ Order + OrderItem 생성
   └─ processed = true 로 업데이트

③ 고객 이벤트 생성
   CustomerEvent: {eventType: 'purchase', occurredAt: orderedAt}
```

### 채널별 API 연동 구조

```
api_connections 테이블에 채널별 자격증명 저장:
  ├─ name: "쿠팡 파트너스"
  ├─ channel: "COUPANG"
  ├─ endpoint: "https://api.coupang.com/v2/..."
  ├─ auth_type: "oauth2"
  └─ token_encrypted: AES-256-CBC 암호화된 토큰
```

---

## 7. 유통기한 알림 프로세스

**목적**: 유통기한이 60일 이내로 임박한 상품을 관리자에게 이메일로 알립니다.

**실행 시점**: 매일 오전 9시 (자동)

### 처리 흐름

```
매일 09:00 실행
    │
    ▼
① SKU 마스터에서 유통기한 임박 상품 조회:
   조건: 오늘 ≤ lot_expiry ≤ 오늘 + 60일
    │
    ▼
② 각 상품별 D-day 계산 및 분류:

   D-7 이내  → 🔴 긴급 (빨간색)
   D-30 이내 → 🟠 주의 (주황색)
   D-60 이내 → 🟡 경고 (노란색)
    │
    ▼
③ 이메일 HTML 테이블 구성:
   | D-tag | 상품명 | SKU코드 | 유통기한 | 잔여재고 |
    │
    ▼
④ 관리자 이메일 목록 조회:
   users WHERE role IN ('super_admin', 'admin') AND is_active = true
    │
    ▼
⑤ 각 관리자에게 이메일 발송 (Resend):
   제목: "[FlowIT] 유통기한 임박 SKU {N}건"
   본문: HTML 테이블
```

### 이메일 예시

```
제목: [FlowIT] 유통기한 임박 SKU 3건

┌────────┬─────────────┬──────────┬─────────────┬─────────┐
│ D-tag  │ 상품명       │ SKU코드  │ 유통기한     │ 잔여재고 │
├────────┼─────────────┼──────────┼─────────────┼─────────┤
│ 🔴 D-3 │ 선크림 SPF50 │ SKU-001  │ 2025-01-08  │ 240개   │
│ 🟠 D-15│ 수분크림     │ SKU-042  │ 2025-01-20  │ 85개    │
│ 🟡 D-45│ 토너 200ml   │ SKU-108  │ 2025-02-19  │ 320개   │
└────────┴─────────────┴──────────┴─────────────┴─────────┘
```

---

## 8. API 헬스 체크 프로세스

**목적**: 연동된 외부 채널 API가 정상 작동하는지 매일 자동 점검합니다.

**실행 시점**: 매일 배치 작업 내 포함 (새벽 3시)

### 처리 흐름

```
① api_connections 테이블의 모든 연결 조회
    │
    ▼
② 각 연결별 상태 확인:
   HEAD {endpoint} 요청 (타임아웃: 5초)
    │
    ├─ 응답 성공 + 지연시간 < 500ms → 상태: 'connected'
    ├─ 응답 성공 + 지연시간 ≥ 500ms → 상태: 'delayed'
    ├─ HTTP 에러 응답              → 상태: 'error'
    └─ 타임아웃 / 네트워크 오류    → 상태: 'error'
    │
    ▼
③ 결과 저장:
   api_connections 테이블 업데이트:
   ├─ status: 'connected' | 'delayed' | 'error'
   ├─ last_latency_ms: 응답 시간 (ms)
   ├─ last_checked_at: 체크 시각
   └─ error_message: 오류 발생 시 메시지
```

---

## 9. 자동화 배치 일정 (Cron)

### 전체 배치 스케줄

| 시각 (KST) | 작업 | 엔드포인트 |
|-----------|------|-----------|
| 매시간 정각 | 여정 자동화 실행 | `/api/cron/journey-runner` |
| 매일 02:00 | VOC 리뷰 AI 분석 | `/api/cron/voc-analyze` |
| 매일 03:00 | RFM 점수 재계산 | `/api/cron/rfm` |
| 매일 09:00 | 유통기한 알림 발송 | `/api/cron/lot-expiry` |
| 매일 03:00 | 위 전체를 통합 실행 | `/api/cron/daily` |

> **Vercel 무료 플랜 사용 시**: 개별 Cron 대신 `/api/cron/daily` 하나만 설정하여 모든 작업을 순차 실행합니다.

### 일일 배치 실행 순서

```
/api/cron/daily 실행 (03:00 KST)

순차 실행:
  1️⃣  VOC 분석
      ↓ 완료
  2️⃣  RFM 계산 (가장 시간이 오래 걸림)
      ↓ 완료
  3️⃣  여정 자동화
      ↓ 완료
  4️⃣  유통기한 알림
      ↓ 완료
  5️⃣  API 헬스 체크

최종 결과 반환:
{
  "voc":        "ok" or "error: ...",
  "rfm":        "ok" or "error: ...",
  "journey":    "ok" or "error: ...",
  "lotExpiry":  "ok" or "error: ...",
  "healthCheck":"ok" or "error: ..."
}
```

### Cron 보안

모든 Cron 엔드포인트는 Authorization 헤더로 보호됩니다:

```
요청 헤더: Authorization: Bearer {CRON_SECRET}
환경변수:  CRON_SECRET=xxxxxxxxxxxx
```

올바른 Secret 없이 호출 시 → `401 Unauthorized` 반환

---

## 10. 사용자 초대 및 온보딩 프로세스

**목적**: 새 팀원을 FlowIT CRM에 초대하고 계정을 생성합니다.

### 전체 흐름

```
관리자 → 계정 관리 페이지에서 "초대 발송"
            │
            ▼
① 초대 토큰 생성 (32바이트 랜덤 hex 문자열)
   InviteToken 저장:
   ├─ token: 고유 토큰값
   ├─ email: 초대할 이메일 주소
   ├─ role: 부여할 권한 (member, manager 등)
   ├─ group_id: 소속 그룹 (선택)
   ├─ expires_at: 현재 시각 + 7일
   └─ used_at: null (미사용)
            │
            ▼
② 초대 이메일 발송 (Resend):
   수신: 입력한 이메일 주소
   제목: "FlowIT CRM 초대"
   내용: 초대 링크 (7일 후 만료)
   링크: https://{도메인}/invite/{token}
            │
            ▼
③ 초대 받은 사람이 링크 클릭
            │
            ▼
④ /invite/{token} 페이지 접속
   ├─ 이름 입력 (최소 2자)
   ├─ 비밀번호 입력 (최소 8자)
   └─ 비밀번호 확인 입력
            │
            ▼
⑤ POST /api/auth/register 요청
   ├─ 토큰 유효성 검증 (만료 여부, 사용 여부)
   ├─ 비밀번호 bcrypt 해시 처리
   ├─ users 테이블에 신규 사용자 생성
   │   ├─ email: 초대 시 입력된 이메일
   │   ├─ name: 직접 입력한 이름
   │   ├─ password_hash: bcrypt 해시
   │   ├─ role: 초대 시 설정된 역할
   │   └─ group_id: 초대 시 설정된 그룹
   └─ invite_tokens.used_at = 현재 시각 (토큰 소모)
            │
            ▼
⑥ 로그인 페이지로 이동 → 생성된 계정으로 로그인
```

### 초대 토큰 만료 정책

| 상황 | 결과 |
|------|------|
| 7일 이내 사용 | 정상 가입 |
| 7일 초과 | 만료 오류 → 관리자에게 재초대 요청 |
| 이미 사용된 토큰 | 오류 → 이미 가입된 계정으로 로그인 |

---

## 11. 감사 로그 기록 프로세스

**목적**: 데이터를 변경하는 모든 작업(생성, 수정, 삭제, 발송 등)을 자동으로 기록합니다.

**작동 방식**: API 핸들러를 `withAuditLog()` 미들웨어로 감싸면 자동 기록

### 기록 흐름

```
사용자가 데이터 변경 API 호출
            │
            ▼
① API 핸들러 실행 (원래 로직 수행)
            │
            ▼
② 응답 상태 확인
   ├─ 실패 (4xx/5xx) → 감사 로그 미기록 (작업 미완료)
   └─ 성공 (2xx) → 감사 로그 기록
            │
            ▼
③ 감사 로그 데이터 수집:
   ├─ userId: 현재 로그인한 사용자 ID
   ├─ action: 작업 유형 (CREATE/UPDATE/DELETE/SEND 등)
   ├─ resource: 작업 대상 리소스 (customer/campaign/deal 등)
   ├─ resource_id: 대상 레코드 ID
   ├─ ip: 클라이언트 IP (x-forwarded-for 헤더)
   ├─ user_agent: 브라우저 정보
   ├─ payload_before: 변경 전 데이터 (JSON)
   └─ payload_after: 변경 후 데이터 (JSON)
            │
            ▼
④ audit_logs 테이블에 저장 (비동기, 원래 응답에 영향 없음)
            │
            ▼
⑤ 원래 API 응답 그대로 반환
```

### 기록 대상 작업

| 액션 | 설명 | 위험도 |
|------|------|--------|
| CREATE | 새 데이터 생성 | 낮음 |
| UPDATE | 데이터 수정 | 낮음 |
| DELETE | 데이터 삭제 | **높음** 🔴 |
| SEND | 캠페인 발송 | **높음** 🔴 |
| INVITE | 사용자 초대 | **높음** 🔴 |
| EXPORT | 데이터 내보내기 | **높음** 🔴 |
| LOGIN | 로그인 | 낮음 |
| LOGOUT | 로그아웃 | 낮음 |

---

## 12. 영업 파이프라인 단계 변경 프로세스

**목적**: 거래 카드를 드래그하여 단계를 변경하면 즉시 DB에 반영됩니다.

### 드래그&드롭 처리 흐름

```
사용자가 거래 카드를 드래그
            │
            ▼
① 드래그 시작 (DndContext)
   └─ active.id = 드래그 중인 거래 ID
            │
            ▼
② 다른 컬럼 위로 이동
   └─ over.id = 목표 단계 ID (LEAD/MEETING/QUOTE/REVIEW/CLOSED)
            │
            ▼
③ 드롭 완료 (handleDragEnd)
   
   유효성 검사:
   ├─ 같은 컬럼에 드롭? → 변경 없음, 종료
   └─ 다른 컬럼에 드롭? → 계속
            │
            ▼
④ UI 즉시 업데이트 (낙관적 업데이트)
   └─ deals 상태에서 해당 거래의 stage를 바로 변경
      (사용자는 즉시 결과 확인, 로딩 없음)
            │
            ▼
⑤ 백엔드 동기화 (비동기)
   └─ PATCH /api/deals/{id}
      { "stage": "MEETING" }
            │
      ├─ 성공 → UI 상태 유지
      └─ 실패 → UI 롤백 + 오류 표시
```

### 단계별 예상 매출 계산

```
예상 매출 = Σ (deal.amount × deal.probability / 100)

CLOSED(성사) 단계는 제외
(이미 확정된 매출이므로 별도 집계)

예시:
  잠재단계: 500만원 × 20% = 100만원
  미팅단계: 300만원 × 50% = 150만원
  견적단계: 200만원 × 70% = 140만원
  검토단계: 100만원 × 90% = 90만원
  ─────────────────────────
  총 예상 매출: 480만원
```

---

## 13. 전체 데이터 흐름

아래는 FlowIT CRM 전체 시스템의 데이터 흐름을 보여줍니다.

```
┌─────────────────────────────────────────────────────────────┐
│                      외부 채널                               │
│  [자체몰]  [쿠팡]  [네이버]  [올리브영]  [인스타그램]         │
└────┬──────────┬────────────────────────────────────────────┘
     │ Webhook  │ API Sync
     ▼          ▼
┌────────────────────────────────────────────────┐
│              주문 수집 레이어                     │
│  channel_orders_raw (원본 데이터 버퍼)            │
└───────────────────┬────────────────────────────┘
                    │ 주문 정규화 처리
                    ▼
┌────────────────────────────────────────────────┐
│              고객 통합 식별                       │
│  Identity Resolution (채널ID→이메일→전화→주소)   │
└───────────┬────────────────┬───────────────────┘
            │                │
            ▼                ▼
┌───────────────────┐  ┌─────────────────┐
│  신규 고객 생성     │  │  기존 고객 연결   │
│  customers        │  │  customer_       │
│  customer_        │  │  identities      │
│  identities       │  │  (채널 ID 추가)  │
└─────────┬─────────┘  └────────┬────────┘
          └──────────┬──────────┘
                     ▼
┌────────────────────────────────────────────────┐
│              통합 고객 데이터 저장                │
│  orders / order_items / customer_events        │
└───────────────────┬────────────────────────────┘
                    │
         ┌──────────┼──────────────┐
         ▼          ▼              ▼
┌─────────────┐ ┌──────────┐ ┌──────────────┐
│  매일 03:00  │ │매시간 실행│ │  매일 02:00  │
│  RFM 계산    │ │여정 엔진  │ │  VOC 분석    │
│  세그먼트    │ │자동 메시지│ │  AI 감정분석 │
│  LTV 업데이트│ │캠페인 발송│ │  이상징후탐지│
└──────┬──────┘ └────┬─────┘ └──────┬───────┘
       │             │              │
       └─────────────┼──────────────┘
                     ▼
┌────────────────────────────────────────────────┐
│              대시보드 / 분석                      │
│  메인KPI · 채널별매출 · 고객세그먼트 · BI분석     │
└────────────────────────────────────────────────┘
                     │
                     ▼
┌────────────────────────────────────────────────┐
│              감사 & 보안                          │
│  audit_logs: 모든 데이터 변경 자동 기록           │
│  (액션 · 사용자 · IP · 변경 전후 데이터)          │
└────────────────────────────────────────────────┘
```

### 핵심 자동화 사이클 요약

| 주기 | 자동화 내용 | 목적 |
|------|-----------|------|
| 실시간 | 웹훅 수신 → 주문 저장 | 주문 누락 방지 |
| 1시간 | 여정 엔진 실행 | 고객 액션 자동화 |
| 매일 02:00 | VOC AI 분석 | 리뷰 품질 모니터링 |
| 매일 03:00 | RFM 재계산 | 고객 세그먼트 최신화 |
| 매일 09:00 | 유통기한 알림 | 재고 손실 예방 |
| 수시 | API 헬스 체크 | 채널 연동 안정성 확보 |

---

## 환경변수 참고

| 변수명 | 용도 | 예시 |
|--------|------|------|
| `DATABASE_URL` | PostgreSQL 연결 URL | `postgresql://...` |
| `AUTH_SECRET` | JWT 서명 키 | 랜덤 32자 이상 문자열 |
| `CRON_SECRET` | Cron 엔드포인트 보안 | 랜덤 문자열 |
| `RESEND_API_KEY` | 이메일 발송 API 키 | `re_xxxxxxxxxx` |
| `EMAIL_FROM` | 발신 이메일 주소 | `noreply@flowit.kr` |
| `ENCRYPTION_KEY` | API 토큰 암호화 키 | 64자리 hex 문자열 |
| `ENCRYPTION_IV` | AES 초기화 벡터 | 32자리 hex 문자열 |
| `SLACK_WEBHOOK_URL` | VOC 이상 징후 알림 | `https://hooks.slack.com/...` |
| `ANTHROPIC_API_KEY` | Claude AI API 키 | `sk-ant-...` |

---

*FlowIT CRM v0.1.0 | 프로세스 메뉴얼*
